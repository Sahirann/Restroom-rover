import { useCallback } from "react";
import styles from "./card.scoped.css";

const Bally = () => {
  const onDirectionContainerClick = useCallback(() => {
    // Please sync "direct to bally" to the project
  }, []);

  return (
    <div className="card">
       
    </div>
  );
};

export default card;
